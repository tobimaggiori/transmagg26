# CLAUDE.md

IMPORTANTE: Antes de hacer git push, SIEMPRE ejecutá npm run build. Si el build falla, analizá el error, corregí el código, y volvé a ejecutar npm run build. Repetí este ciclo hasta que el build pase exitosamente. Solo entonces hacé git push.
